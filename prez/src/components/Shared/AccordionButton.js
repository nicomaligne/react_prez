import React from 'react'
import PropTypes from 'prop-types'
import styled from 'styled-components'

const Button = styled.button`
	padding: 10px;
`

function AccordionButton() {
	return <Button value="test" />
}
